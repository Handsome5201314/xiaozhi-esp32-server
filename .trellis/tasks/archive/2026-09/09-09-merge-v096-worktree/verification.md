# 验证结果

## 来源与整合范围

- 原分支从 v0.9.6 标签创建于 2026-08-21；历史计划记录医疗录音接口接入。
- 源分支 093676e5 / b0dc0bbc / a1d85a98 的功能已对应移植到 main 的 5a16903c / 623ba872 / be8e688a。
- 医疗模块与既有测试的分支已提交内容完全相同；connection.py 存在上游差异，因此只应用相关补丁。
- 整合三处生产文件修复、更新网关测试、移入两份新增测试与历史计划；新增非法输入与真实超时恢复回归覆盖。
- 未整条合并 server-v0.9.6 的上游升级。

## 测试证据

使用已有镜像 xiaozhi-esp32-server:v0.9.6-medical-gpu-bindwait-20260823 作为 Python 3.10 依赖环境；测试代码来自主目录副本，禁网，无生产 data/models 挂载，容器退出自动删除。

命令：`python -m unittest discover -s tests -v`。

- 修复前：12 个测试，3 个失败（慢绑定丢消息、空会话返回 422、序号错误文案差异）。
- 修复后：14 个测试全部通过，耗时 11.530 秒。
- 覆盖认证、设备归属、WebSocket 帧 ACK、Ogg 落盘、空录音、非法输入、慢查询、超时恢复、OTA 路由、本地医疗配置保留与 FunASR CUDA 配置。
- CUDA 测试沿用原有边界替身，不代表实际 GPU 推理验证；未部署此次主目录代码。

## 保全与运行限制

变更前保存了 277 个既有文件的 SHA-256，备份在 `.git/merge-backups/20260909-v096`（原目标文件、源补丁、哈希清单与隔离测试副本）。

运行容器 xiaozhi-esp32-server-v096-medical 仍挂载旧目录 main/xiaozhi-server/data。此数据包含运行配置和医疗数据，本次未复制或覆盖；迁移需用户确认短暂停机。旧目录和分支均保留，不可直接删除。主目录其他未提交业务内容不纳入本次提交。

最终保全检查：277 个既有文件哈希全部一致；三处生产补丁反向 git apply --check 通过；git diff --check 通过。隔离测试容器已自动删除，运行服务启动时间未变。
