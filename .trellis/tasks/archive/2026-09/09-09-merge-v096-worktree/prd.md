# 整合 v0.9.6 工作目录遗留修复

## 目标与验收

用户要求查清旧工作目录用途，并把开发成果合并到主目录。医疗网关和 FunASR CUDA 的三次提交已于 2026-08-22 移植到 main，本次整合剩余四个已修改文件、两个新增测试与一份历史计划。

- 空录音会话使用 last_sequence=-1 正常结束；小于 -1 的序号仍被拒绝。
- 智控台查询耗时超过 1 秒时继续等候；10 秒超时只记录真实状态，不误报设备未绑定。
- 保留主目录所有既有未提交文件；旧目录保留可回退副本。
- 运行行为测试、差异检查，记录未验证项目。

## 范围

本次不是将 main 升级为上游 v0.9.6（两分支相差 297 个文件）。旧目录 data 正由运行容器挂载；迁移数据与容器挂载已单独询问用户，在获得答复前不变更运行服务。
