# 执行与验证

1. [x] 核对分支历史、工作树补丁和运行容器挂载。
2. [x] 保存目标文件副本、补丁及无关脏文件哈希。
3. [x] 先移入遗留测试，观察空会话与慢绑定在主目录复现失败。
4. [x] 应用两个行为修复，移入历史计划。
5. [x] 运行 Python 相关测试、git diff --check、文件保全校验。
6. [x] 记录整合结果和运行目录保留/迁移决定。

测试边界：网关测试使用 aiohttp 实际 HTTP/WebSocket 与临时磁盘；SessionStore 使用真实文件；遗留绑定测试在网络配置查询完成信号边界驱动 Event，替换文本路由接收端验证消息流。测试镜像提供真实依赖，不替换模块导入或用 mock 数据库伪造运行。

已知工具限制：本会话没有 fast-context 工具，使用 Git 历史、精确 rg 和完整相关模块阅读补足。
